import { GetRequestParameters } from './get-request-parameters';

describe('GetRequestParameters', () => {
  it('should create an instance', () => {
    expect(new GetRequestParameters()).toBeTruthy();
  });
});
